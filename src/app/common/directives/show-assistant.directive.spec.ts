import { ShowAssiatantDirective } from './show-assistant.directive';

describe('ShowAssiatantDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAssiatantDirective();
    expect(directive).toBeTruthy();
  });
});
